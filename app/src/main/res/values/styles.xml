<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="ListItem" parent="@style/AppTheme">
        <item name="android:textSize">20sp</item>
        <item name="android:layout_height">32sp</item>
        <item name="android:padding">3sp</item>
    </style>

    <style name="List" parent="@style/AppTheme">
        <item name="android:layout_height">96sp</item>
        <item name="android:padding">0sp</item>
        <item name="android:layout_margin">3dp</item>
    </style>

</resources>
